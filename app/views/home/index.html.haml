%h1
  A Cure for Writer's Block!

.image_upload
  = form_for @upload, url: upload_path do |form|
    = form.file_field :image
    = form.submit 'upload'

.past_phrases
  - @phrases.each do |p|
    .image
      = image_tag(p.upload.image.url(:thumb))
    .phrase
      = p.content
      <br>
